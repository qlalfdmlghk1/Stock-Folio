# Component Creation Skill

When creating a Vue component:
1. Create the .vue file with TypeScript <script setup>
2. Add null-safe prop handling with defaults
3. Create corresponding .stories.ts with autodocs
4. Add any needed CSS in scoped style block
